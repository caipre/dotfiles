[user]
   name = Nick Platt
   email = platt.nicholas@gmail.com
[alias]
   lg = log --pretty=tformat:'%C(yellow)%h%C(reset)  %C(bold black)%<(20,trunc)%an%C(reset)  %<(80,trunc)%s'
   can = commit --amend --no-edit
   stat = show --stat
   tags = tag -l
   branches = branch -vv
   remotes = remote -v
[color]
   ui = always
[core]
   excludesfile = ~/.gitignore
   pager = cat && echo
[diff]
   statGraphWidth = 20
[pull]
   rebase = true
[push]
   default = current
[fetch]
   prune = true
