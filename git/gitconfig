[user]
   name = Nick Platt
   email = platt.nicholas@gmail.com
   signingkey = E06ABC4FF0A690FC
[include]
   path = ~/.gitconfig.profile
[includeIf "gitdir:~/code/github/caipre/"]
   path = ~/.gitconfig.personal
[alias]
   lg = log --pretty=tformat:'%C(yellow)%h%C(reset) %C(bold black)%<(10,trunc)%an%C(reset) %<(50,trunc)%s'
   can = commit --amend --no-edit
   stat = show --stat
   tags = tag -l
   branches = branch -vv
   remotes = remote -v
   blameless = blame
   up = !git fetch && git rebase && git sub
   sub = submodule update --init --checkout --force --recursive
[color]
   ui = always
[core]
   excludesfile = ~/.gitignore
   pager = less -FXRi && echo
[diff]
   statGraphWidth = 20
[pull]
   rebase = true
[push]
   default = current
[fetch]
   prune = true
   pruneTags = true
[commit]
   gpgsign = true
[rerere]
   enabled = true
