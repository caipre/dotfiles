[user]
   name = Nick Platt
   email = platt.nicholas@gmail.com
   signingkey = E06ABC4FF0A690FC
[includeIf "gitdir:~/code/spotify/"]
   path = ~/.gitconfig.spotify
[includeIf "gitdir:~/code/github/spotify/"]
   path = ~/.gitconfig.spotify
[alias]
   lg = log --pretty=tformat:'%C(yellow)%h%C(reset) %C(bold black)%<(10,trunc)%an%C(reset) %<(50,trunc)%s'
   can = commit --amend --no-edit
   stat = show --stat
   tags = tag -l
   branches = branch -vv
   remotes = remote -v
[color]
   ui = always
[core]
   excludesfile = ~/.gitignore
   pager = less -FXRi && echo
[diff]
   statGraphWidth = 20
[pull]
   rebase = true
[push]
   default = current
[fetch]
   prune = true
[commit]
   gpgsign = true
